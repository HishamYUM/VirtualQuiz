<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlineexamination">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TermsAndConditions"></activity>
        <activity android:name=".SignupPage" />
        <activity android:name=".DummyPage" />
        <activity android:name=".NewPage" />
        <activity android:name=".SelectCourse" />
        <activity android:name=".result" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomePage" />
        <activity android:name=".AddQuestions" />
        <activity android:name=".ChoicePage" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".UserCourse" />
        <activity android:name=".FrontPage" />
        <activity android:name=".screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>